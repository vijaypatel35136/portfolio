# Production (GitHub Pages) - Uses localStorage only
# No backend needed! Settings save to browser storage and persist automatically

# Leave this empty to disable backend API calls on GitHub Pages
REACT_APP_API_URL=
