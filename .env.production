# Production (GitHub Pages) - Uses localStorage only
# No backend needed! Settings save to browser storage and persist automatically

# Do NOT set VITE_API_URL on GitHub Pages
# This will disable all backend API calls
# If you deploy a backend later, add: VITE_API_URL=https://your-backend-url/api
